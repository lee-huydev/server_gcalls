import styled from "styled-components";

export const Option = styled.div`
     display: flex;
      color: #fff;
      font-size: 18px;
      font-weight: 500;
      gap: 20px;
      cursor: pointer;
      .item {
         padding: 5px 10px;
         border: none;
         border-radius: 10px;
      }
`